import { useQuery } from '@tanstack/react-query';
import { Bar, BarChart, CartesianGrid, Line, LineChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import { api } from '../api';
export default function Dashboard(){ const stats=useQuery({queryKey:['stats'],queryFn:()=>api<any>('/admin/stats')}); const demo=Array.from({length:30},(_,i)=>({day:i+1,users:Math.round(5+Math.sin(i/3)*4+i/4),watch:Math.round(20+Math.cos(i/5)*8+i)})); return <><h1>Dashboard</h1><div className="stats"><Card title="Total Content" value={stats.data?.totalContent}/><Card title="Total Users" value={stats.data?.totalUsers}/><Card title="Active Sessions" value={stats.data?.activeSessions}/><Card title="Storage Used" value={`${((stats.data?.storageBytes??0)/1024/1024/1024).toFixed(2)} GB`}/></div><div className="charts"><div className="panel"><h2>New users</h2><ResponsiveContainer height={260}><LineChart data={demo}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="day"/><YAxis/><Tooltip/><Line dataKey="users" stroke="currentColor"/></LineChart></ResponsiveContainer></div><div className="panel"><h2>Top content by watch time</h2><ResponsiveContainer height={260}><BarChart data={demo.slice(0,8)} layout="vertical"><XAxis type="number"/><YAxis dataKey="day" type="category"/><Tooltip/><Bar dataKey="watch" fill="currentColor"/></BarChart></ResponsiveContainer></div></div></>; }
function Card({title,value}:{title:string;value:any}){return <div className="stat-card"><span>{title}</span><strong>{value ?? '—'}</strong></div>}
